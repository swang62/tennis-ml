node_modules/
dist/
# Written by deploy.py before the web image build; consumed (deleted) by
# scripts/build-player-index.mjs inside the build so it never ships.
/public/player-directory.json
# Generated by scripts/build-player-index.mjs during the web image build:
# the content-hashed payload plus the discovery manifest.
/public/player-index.*.json
apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: tennis-ml

servers: 1
agents: 0

ports:
  - port: 8080:80
    nodeFilters:
      - loadbalancer
  - port: 8123:30812
    nodeFilters:
      - loadbalancer

options:
  k3d:
    wait: true
    timeout: "120s"
